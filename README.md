# streamlit
Follow the link to create a virtual environment: https://code.visualstudio.com/docs/python/python-tutorial
Activated virtual environment (use Windows command prompt): .venv\Scripts\activate.bat
Update pip: python -m pip install --upgrade pip
Update requirements.txt (only if you need to upgrade): pip install -r requirements.txt